apiVersion: v1
kind: Service
metadata:
  name: nginx
  labels:
    app: web
spec:
  clusterIP: None        # This makes it a Headless Service
  selector:
    app: web
  ports:
  - port: 80
    name: web