apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: capstone
  labels:
    app: mysql
spec:
  clusterIP: None  # Headless Service - no ClusterIP
  selector:
    app: mysql
  ports:
  - port: 3306
    targetPort: 3306
    protocol: TCP
